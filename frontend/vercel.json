{
  "framework": "nextjs",
  "installCommand": "cd .. && npm ci --workspace=shared --workspace=frontend",
  "buildCommand": "cd .. && npm run build --workspace=shared && npm run build --workspace=frontend"
}
